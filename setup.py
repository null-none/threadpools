from setuptools import setup, find_packages

setup(
    name="threadpools",
    version="1.0.0",
    author="Dmitry Kalinin",
    packages=["threadpools"],
    url="https://github.com/null-none/threadpools",
    keywords=[
        "thread",
        "pool",
        "thread pool"
        "thread pools"
    ],
    install_requires=[],
)
